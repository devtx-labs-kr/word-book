# Tests use an in-memory H2 so they never touch the ./data file store.
spring.datasource.url=jdbc:h2:mem:wordbook-test;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.open-in-view=false
